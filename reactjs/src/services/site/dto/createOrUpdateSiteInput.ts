export interface CreateOrUpdateSiteInput {
  id: number;
  siteName: string;
  siteCode: string;
  location: string;
  contactPerson: string;
  contactNumber: string;
  // state: number;
  // insterState: number;
  // longitude: string;
  // latitude: string;
}
